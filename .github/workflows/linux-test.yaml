name: GitHub Action Demo

on:
  push:

jobs:
  action-demo:
    name: demo-test
    runs-on: ubuntu-latest

    steps:
        - run: pwd

        - run: whoami

        - run: hostname

        - run: printenv

    
